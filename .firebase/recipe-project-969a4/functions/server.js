import { onRequest } from 'firebase-functions/v2/https';
  const server = import('firebase-frameworks');
  export const ssrrecipeproject969a4 = onRequest({"region":"us-west1"}, (req, res) => server.then(it => it.handle(req, res)));
  